### Install workflow fails with 404 error when using tags

## Problem

The Install Script Test workflow is failing with a 404 error when triggered on tags/releases. 

**Failed Job:** https://github.com/calypr/git-drs/actions/runs/21888122076/job/63187816255

**Error:**
```
curl: (56) The requested URL returned error: 404
##[error]Process completed with exit code 1.
```

## Root Cause

The workflow in `.github/workflows/install.yaml` (line 28) constructs the install script URL as:

```bash
https://raw.githubusercontent.com/calypr/git-drs/refs/heads/$BRANCH/install.sh
```

When `$BRANCH` is a tag like `v0.5.13`, this creates an invalid URL:
- ❌ `https://raw.githubusercontent.com/calypr/git-drs/refs/heads/v0.5.13/install.sh`

Tags exist at `refs/tags/`, not `refs/heads/`, causing the 404.

## Solution

Remove the hardcoded `/refs/heads/` path from the URL. GitHub's raw content URLs work with ref names directly:

```yaml
- name: Install Default Version (Latest Release)
  run: |
    BRANCH=${{ github.ref_name }}
    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/calypr/git-drs/$BRANCH/install.sh)"
    source $HOME/.bashrc
    which git-drs
    git-drs --help
```

This will work for both branches and tags.